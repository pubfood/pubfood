<?js
    var data = obj;
    var self = this;
    data.forEach(function(name, i) {
    var text = name.replace(/([a-z]+)(\#|\~)([a-z]+)(\.)/, '');
?>
<span class="param-type"><?js= self.linkto(name, self.htmlsafe(text)) ?></span>
<?js if (i < data.length-1) { ?>|<?js } ?>
<?js }); ?>